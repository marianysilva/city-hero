# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
# expo-env.d.ts is NOT ignored — Expo's own docs say to commit it (it's what
# makes `import "../global.css"` etc. typecheck without needing the Expo CLI
# to have run first). See docs/tasks/00-foundation/02-design-tokens.md.

# NativeWind
# nativewind-env.d.ts is NOT ignored — same reasoning, per NativeWind's own
# TypeScript setup docs ("should be committed with your source code").

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli
# expo-env.d.ts removed from this block — see the comment near the top of
# this file: Expo's own docs say to commit it, and it's already tracked in
# git, so ignoring it here was a no-op that only contradicted that comment.
# @end expo-cli