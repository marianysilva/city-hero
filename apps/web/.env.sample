# Only needed when running web outside Docker (locally).
# When using docker-compose, the root .env is used instead.

NEXT_PUBLIC_API_URL=http://localhost:8000

# Must match backend SECRET_KEY — used by proxy.ts to verify JWTs in Edge runtime.
SECRET_KEY=
