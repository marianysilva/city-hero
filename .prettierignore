node_modules
.next
.expo
dist
build
coverage
package-lock.json
*.svg

# Build/test-run output — never committed, not project-authored
storybook-static
test-results
playwright-report

# Python virtualenvs (not project-authored, vendored third-party packages)
.venv/

# Auto-generated type declarations
expo-env.d.ts
nativewind-env.d.ts

# Vendored third-party skill packages — not project-authored, reformatting
# them creates noise and diverges from upstream on the next sync.
.agents/

# Claude Code's own tooling config (agent/skill definitions) — managed by
# the harness, not this project's Markdown prose-wrap standard (see the
# "Markdown / documentation" section in docs/engineering/coding-standards.md).
.claude/
